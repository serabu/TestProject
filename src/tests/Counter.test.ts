// import configureStore from "redux-mock-state";

// describe("Home", () => {
//   const middlewares: never[] = [];
//   const mockStore = configureStore(middlewares);
//   const UpdateCountAction = () => ({ type: "UPDATE_COUNT_ACTION" });

//   it("should be dispatch actions", () => {
//     const initialState = {};
//     const state = mockStore(initialState);
//     state.dispatch(UpdateCountAction());
//     const actions = state.getActions();
//     const expectedPayload = { type: "UPDATE_COUNT_ACTION" };
//     expect(actions).toEqual([expectedPayload]);
//   });
// });
